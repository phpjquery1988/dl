# USA Driver License Scanner — Python Dependencies
# Install with:  pip install -r requirements.txt

# Core barcode engine — best open-source PDF417 reader for Python
# Wraps the high-performance C++ zxing-cpp library
zxing-cpp>=2.2.0

# Computer vision — camera capture, image preprocessing, display window
opencv-python>=4.8.0

# Image processing helpers
Pillow>=10.0.0

# Numerical operations (used by OpenCV preprocessing)
numpy>=1.24.0
